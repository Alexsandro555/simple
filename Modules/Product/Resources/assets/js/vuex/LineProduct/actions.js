import { GLOBAL } from "@/constants";
import { ACTIONS, PRIVATE } from "@product/constants"
import { api } from "../../api/LineProduct";

export default {
    /*[ACTIONS.SAVE]: ({commit, store},data) => {
    }*/
}